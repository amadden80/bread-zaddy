{
  "name": "bread-zaddy",
  "version": "1.0.0",
  "private": true,
  "description": "Bread Zaddy — two vibe-coded games behind one device-aware page.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "python3 -m http.server 8000",
    "dev": "node build.mjs && python3 -m http.server 8000"
  }
}
